'use strict';

(function (angular) {
    angular
        .module('soundcloudPluginDesign')
        .controller('DesignHomeCtrl', ['$scope', '$timeout',
            function ($scope, $timeout) {
                /*
                 * Private variables
                 *
                 * _data used to specify default values to when user's data doesn't exist.
                 * @type {object}
                 * @private
                 *
                 * tmrDelay used to hold the time returned by $timeout
                 * @private
                 *
                 *  */
                var DesignHome = this;
            }])});